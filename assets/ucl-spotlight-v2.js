/* Legacy Spotlight v2 retired.
   Spotlight rendering is owned exclusively by ucl-spotlight-v3.js.
   Kept as a no-op so cached index.html versions cannot start a second renderer. */
(() => {
  "use strict";
})();